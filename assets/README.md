Lege hier optional eine Datei `app.ico` ab, wenn die erzeugte `SilentInstallHelper.exe`
ein eigenes Windows-Icon erhalten soll.

Erwarteter Pfad:

- `assets/app.ico`

Wenn keine Icon-Datei vorhanden ist, verwendet der Build das Standard-Icon.
